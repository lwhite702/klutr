import { AdminSystemHealth } from "@/components/admin/pages/AdminSystemHealth";

/**
 * Admin System Health Page
 * 
 * Displays:
 * - Health check cards (AI Gateway, Database, Env, Errors)
 */
export default function AdminSystemHealthPage() {
  return <AdminSystemHealth />;
}

